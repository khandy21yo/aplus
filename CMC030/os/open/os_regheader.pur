	!======================================================================
	! OS_REGHEADER file (create, open read/write purge)
	!======================================================================

	CALL ASSG_CHANNEL(OS_REGHEADER.CH_OLD%, STAT%)
	CALL READ_DEVICE('OS_REGHEADER',OS_REGHEADER.DEV$, STAT%)
	CALL READ_PROTECTION('OS_REGHEADER',OS_REGHEADER.PRO$,STAT%)
	CALL READ_CURPROTECTION(OLD_PROT$,STAT%)
	CALL WRIT_CURPROTECTION(OS_REGHEADER.PRO$, STAT%)

	OS_REGHEADER.NAME_OLD$ = OS_REGHEADER.DEV$+"OS_REGHEADER.HIS_OLD"

	OPEN OS_REGHEADER.NAME_OLD$ FOR OUTPUT AS FILE OS_REGHEADER.CH_OLD%, &
		ORGANIZATION INDEXED FIXED, &
		MAP OS_REGHEADER, &
		BUFFER 32%, &
		PRIMARY KEY &
			OS_REGHEADER::ORDNUM, &
		ALTERNATE KEY &
		( &
			OS_REGHEADER::ORDTYPE, &
			OS_REGHEADER::ORDNUM &
		)	CHANGES, &
		ALTERNATE KEY &
		( &
			OS_REGHEADER::ORDCAT, &
			OS_REGHEADER::ORDNUM &
		)	CHANGES, &
		ALTERNATE KEY &
		( &
			OS_REGHEADER::CUSNUM, &
			OS_REGHEADER::ORDNUM &
		)	CHANGES, &
		ALTERNATE KEY &
		( &
			OS_REGHEADER::BATCH, &
			OS_REGHEADER::ORDNUM &
		)	CHANGES, &
		ACCESS MODIFY, ALLOW READ

	CALL WRIT_CURPROTECTION(OLD_PROT$, STAT%)

